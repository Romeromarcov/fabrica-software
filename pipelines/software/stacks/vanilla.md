## Estructura
JavaScript/TypeScript vanilla: `src/[modulo]/index.ts`, `src/[modulo]/api.ts` para llamadas HTTP, `src/[modulo]/ui.ts` para DOM.

## Imports
// Vanilla TS — no framework

## Testing
Jest o Vitest.
