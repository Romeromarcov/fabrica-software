## Estructura
Estructura Next.js 14+ (App Router): `app/[modulo]/page.tsx`, `app/[modulo]/layout.tsx`, `components/[Feature].tsx`, `lib/[modulo].ts` para server actions y data fetching.

## Imports
import { Suspense } from 'react';
import type { Metadata } from 'next';

## Testing
Jest + Testing Library + Playwright para E2E.
