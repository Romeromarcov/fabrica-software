langgraph>=0.2.55
authlib>=1.3.0              # GitHub OAuth 2.0
itsdangerous>=2.1.0         # Firma de sesiones (SessionMiddleware)
google-genai>=1.0.0         # SDK nativo Gemini (gemini-3.5-flash con thinking)
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
anthropic>=0.40.0
langgraph-checkpoint-sqlite>=2.0.0
python-dotenv>=1.0.0
rich>=13.7.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0,<3.2.0
httpx>=0.27.0
python-multipart>=0.0.9
pywebpush>=1.14.0           # IX-2: envío de Web Push (VAPID) — import perezoso
aiofiles>=23.0.0
apscheduler>=3.10.0
openai>=1.50.0
# ── Parseo de archivos subidos por el Founder (A0) ────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0
# ── Sandbox de ejecución — herramientas de calidad de código ─────────────────
pytest>=8.0.0           # runner de tests Python
ruff>=0.4.0             # linter + formatter Python (reemplaza flake8)
mypy>=1.10.0            # type-checker Python
scikit-learn>=1.5.0     # VIII-2: routing dinámico TF-IDF
pyyaml>=6.0             # V2 Fase 0: definición de pipelines data-driven (pipeline.yaml)
# ── F0.2: memoria semántica de lecciones (VECTOR_MEMORY_ENABLED=true por defecto) ──
chromadb>=0.5.0               # backend vectorial persistente (fallback keyword si falla)
sentence-transformers>=3.0.0  # embeddings semánticos para la búsqueda de lecciones
# ── F0.1: observabilidad (OTEL_ENABLED=true por defecto; export OTLP a Jaeger) ──
opentelemetry-sdk>=1.27.0                    # TracerProvider + spans por agente
opentelemetry-exporter-otlp-proto-http>=1.27.0  # export OTLP/HTTP a Jaeger/Tempo
langsmith>=0.1.100            # traza del grafo LangGraph en LangSmith (callback)
# ── F5: servidor MCP (expone la fábrica vía Model Context Protocol) ──
mcp>=1.27.0                   # SDK MCP (FastMCP) para mcp_server.py
